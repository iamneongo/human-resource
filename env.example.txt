# =================================================================
# Authentication Configuration (Better Auth + Resend OTP)
# =================================================================
BETTER_AUTH_SECRET=change-me-to-a-long-random-secret
BETTER_AUTH_URL="http://localhost:3000"
AUTH_FROM_EMAIL="HRM <onboarding@resend.dev>"
RESEND_API_KEY=

# =================================================================
# Database Configuration
# =================================================================
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# =================================================================
# Build / App Configuration
# =================================================================
NEXT_PUBLIC_APP_URL="http://localhost:3000"
BUILD_STANDALONE=
NEXT_PUBLIC_SENTRY_DISABLED="false"
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_SENTRY_ORG=
NEXT_PUBLIC_SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# =================================================================
# Important Notes
# =================================================================
# 1. Rename this file to '.env' for local development.
# 2. Nếu chưa cấu hình RESEND_API_KEY, OTP sẽ được log ra console ở môi trường local/dev.
# 3. BETTER_AUTH_SECRET phải là secret mạnh và khác hoàn toàn giữa các môi trường.
